---
description: 较大版本迭代完成后同步更新 GDD 文档
alwaysApply: true
---

# GDD 自动同步规则

项目设计文档路径：`《边境守卫者》游戏开发文档 (GDD).md`（仓库根目录）。

## 何时必须更新 GDD

完成以下任一类型的改动后，**在同一任务内**更新 GDD，不要等用户提醒：

- 新系统 / 新关卡 / 新敌人 / 新炮塔或升级分支
- 地图结构、波次、经济数值的明显调整
- 核心交互流程变化（建造、升级、出售、波次、UI 流程等）
- 跨多个脚本目录的重构，导致设计与实现不一致

以下小改动**不必**更新 GDD：单文件 bug 修复、纯 UI 像素微调、注释、不影响玩法的命名。

## 更新步骤

1. 阅读 GDD 中与本次改动相关的章节。
2. 修改正文，使设计与**当前代码行为**一致；区分「已实现」与「计划中」。
3. 更新文档头部：**版本号**（小改 +0.1，大改 +1.0 或按阶段 +0.1~0.5）、**日期**（YYYY-MM-DD）。
4. 在 **「版本变更记录」** 表格中追加一行：版本、日期、摘要（1~3 条 bullet）。
5. 若涉及实现状态，同步更新 **「八、当前原型实现进度」** 中的 ✅ / 🚧 / ⏳ 标记。

## 写作约定

- GDD 主语言为**中文**；游戏内 UI 文案以**英文**为准，在 GDD 中可标注英文名（如 Ranger / Siege）。
- 数值与代码不一致时，以**当前可玩原型**为准，并在变更记录中注明。
- 不要删除远期规划（如地图二、环境机制），改为标记为「未实现」或「计划中」。

## 版本号建议

| 类型 | 示例 |
|------|------|
| 阶段完成（如敌人系统、地图重做） | 0.2 → 0.3 |
| 单地图 / 单系统大改 | +0.1 |
| 里程碑（可发布 Demo） | 1.0 |
